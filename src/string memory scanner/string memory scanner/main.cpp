#include "..\string memory scanner\scanner\scanner.h"

int main()
{
	
	scanner scanscan(4520); // procress id
	scanscan.scanstring("cat"); // the string that need to be founded

	std::cin.get();
	
}
